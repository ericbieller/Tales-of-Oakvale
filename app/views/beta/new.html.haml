= stylesheet_link_tag 'css/common'
= stylesheet_link_tag 'http://fonts.googleapis.com/css?family=Cambo'

#wrapper
  #signup
    %h1 What is Tales of Oakvale?
    %p
      Tales of Oakvale is a text based MMORPG which puts the players in control of the story line. We're basically taking the  concept of table-top, MUDs, and browser gaming, mixing it all together, and making something awesome ;)
    %h1 Signup for the beta!
    = form_for @betum do |f|
      -if @betum.errors.any?
        #error_explanation
          %h2= "#{pluralize(@betum.errors.count, "error")} prohibited this betum from being saved:"
          %ul
            - @betum.errors.full_messages.each do |msg|
              %li= msg
      = f.label :email
      = f.text_field :email
      = f.submit 'Save'