- if object.errors.any?
  #error_explanation
    %p
      = pluralize(object.errors.count, "thing")
      made our scribes take pause:
    %ul
      - object.errors.full_messages.each do |msg|
        %li= msg